# 安全与敏感内容报告

## 报告范围

- Skill 指令可能鼓励暴力、胁迫、跟踪、诈骗、隐私泄露或绕过同意。
- 提交内容包含真实个人身份、私密聊天或其他敏感数据。
- 提示注入或文档内容可能让代理忽略安全边界、泄露文件或执行未经授权的操作。
- 知识库中的法律、医疗或危机信息存在可能导致现实伤害的严重错误。
- Hermes 插件的 owner、人物/群绑定、渠道、会话令牌或失败关闭边界可被绕过。
- `.local/`、Hermes `.env`、数据库备份、导入归档、临时截图或生成关系投影可能被误提交、越权读取或泄露。

## 报告方式

不要在公开 Issue 中粘贴敏感材料。请使用 GitHub 的 Private vulnerability reporting 功能；如果仓库尚未启用该功能，请只提交不包含隐私的最小公开说明，等待维护者提供私下沟通方式。

报告请包含受影响文件、可复现输入、实际输出、预期安全行为和潜在影响。请对案例中的姓名、账号、照片、联系方式和聊天记录进行匿名化。

## 支持范围

本仓库同时包含可分发 Codex Skill 和面向本机的 Hermes 私有运行时：

- Codex Skill 本身是提示词与知识文档集合，不自行持久化或连接外部系统。
- Hermes 私有运行时接入飞书、外部模型和 WSL/Docker MySQL，并在本机保存人物事件、绑定、快照、owner 个人记忆、备份和只读投影。

MySQL 是关系数据的唯一权威源；`.local/relationships/` 是生成投影，不得直接编辑或提交。凭据存放于宿主保护的 Hermes 环境，不属于仓库。运行时隐私最终还依赖 Hermes/Feishu adapter、Windows ACL、WSL/Docker、外部模型供应商和 operator 配置，这些宿主边界不能只靠仓库提示词保证。

报告运行时问题时只提供最小匿名复现、受影响的源码路径/配置键和已经脱敏的错误类型。不要上传 `.env`、数据库 dump、聊天原文、截图、owner/chat ID、签名令牌或迁移归档。
